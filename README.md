# Elevation Castle

Faithful Feedback professional learning experience.

## GitHub Pages
This folder is ready to publish from the repository root.

Files:
- `index.html` — the complete interactive experience
- `audio/` — optimized sound files used by the site

Do not rename `index.html` or move the `audio` folder unless the HTML references are updated.
